[package]
name = "open-erase-web"
version = "0.1.0"
edition = "2024"

[dependencies]
console_error_panic_hook = "0.1.7"
leptos = { version = "0.8.1", features = ["csr"] }
leptos_router = { version = "0.8.2", features = ["tracing"] }
